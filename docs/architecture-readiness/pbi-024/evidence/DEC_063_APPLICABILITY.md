# Aplicabilidad material de DEC-063

- C02: riesgo alto preservado, sin downgrade;
- C05: owner, migración, FK/check/unique, PostgreSQL, rollback, CAS y cleanup
  verificados;
- C06: source server-side, fail-closed, aislamiento, anti-enumeración,
  sanitización y mutaciones verificados;
- C07: no activada; no hubo release o deploy;
- C08: no activada; no existe waiver.

El expediente habilita revisión formal, no `Done`, `Closed`, merge o release.
