# PBI-041 — Initial Bulk Catalog Composer + Versioned Supplier Intake

## Estado del documento

- **Estado:** Owner Review ready — canonical title + Supplier observed title
  history materialized; Owner Acceptance pending.
- **Naturaleza:** segundo slice vertical de EPIC-015.
- **Readiness:**
  [PASS — Ready for Owner Implementation Authorization](../../quality/evidence/pbi-041/DEFINITION_OF_READY.md),
  2026-09-13.
- **WIP:** seleccionado como único PBI activo por autorización Owner del
  2026-09-14; WIP operacional `1/1`.

## Identificación

| Campo | Valor |
|---|---|
| ID | `PBI-041` |
| Epic | `EPIC-015` |
| Tipo | Product / Persistence / Data / Jobs / Authorization / UI |
| Estado | `Owner Review ready; canonical title/history checkpoint PASS; Owner Acceptance pending` |
| Prioridad | Alta |
| Riesgo | Alto |
| Estimación | `XL — agreed` para el outcome inicial |
| Sprint | `SPRINT-03` |
| Clasificación de sprint | Candidate / Ready |
| Selección operacional | Seleccionado; único WIP activo en checkpoint Owner Review |

### UX-003.1 — Tenant Catalog Field Policy Foundation

La base local materializa un registry finito y una policy Tenant-wide versionada
con fallback retrocompatible, restore explícito, concurrencia optimista y audit
append-only. La API usa contexto de sesión confiable y las capabilities
`catalog.configuration.read/manage` se componen con permisos de costo de
referencia. No hay consumo de Composer, enforcement de Analyze/Apply, snapshot
de Version ni cambio de comportamiento FULL/COMPACT en este checkpoint.

### UX-003.2 — Catalog Field Policy Configuration UI

La configuración tenant-wide se presenta en `Configuración → Lista de precios
→ Campos de carga masiva`. La UI usa exclusivamente el registry y los valores
autoritativos de UX-003.1; campos mínimos se muestran como fijos y Marca,
Descripción y Costo de referencia se editan con un nivel único. Save, restore,
dirty/discard y concurrencia optimista conservan la versión del servidor. La
ruta compone configuración con la capability sensible de costo y sigue fallando
cerrada en backend. No se habilita todavía ningún consumo del Composer.

El proof local autenticado completó Save, reload, descarte de borrador y
restore confirmado. Ese corte UX-003.2 cerró en la versión `3`, con defaults
de producto restaurados y tres eventos append-only; `CatalogItem` y
`SupplierCatalogVersion` no cambiaron durante ese recorrido.

El proof material responsive en Chrome completó `768 px` y `640 px` en claro y
oscuro. La fuente y el contexto de la policy ahora se presentan en líneas
separadas dentro de la tarjeta de estado; filas fijas/configurables, estados,
controles nativos, dirty/discard y acciones permanecieron legibles, alcanzables
y sin overflow horizontal, clipping ni superposición. No se guardó ningún
cambio durante esta comprobación; el consumo del Composer permanecía fuera de
ese checkpoint UX-003.2.

### UX-003.3 — Policy-driven Essentials

El Composer ya lee una proyección operacional segura de la policy efectiva del
Tenant. La lectura exige `catalog.import.prepare`, no otorga ni exige permisos
de configuración, y el servidor omite Costo de referencia si falta
`catalog.reference_cost.read`. En la UI, `Esenciales` se deriva en orden de
registry de `REQUIRED ∪ ESSENTIAL`; `Todas` conserva todos los campos que el
servidor autorizó. Los encabezados `REQUIRED` se anuncian accesiblemente, sin
introducir validación nueva ni cambiar el contrato FULL/COMPACT.

El walkthrough local cambió temporalmente Descripción a Esencial y Marca a
Opcional: tras iniciar una carga efímera, Esenciales mostró Tipo, Título,
Descripción, Categoría, Costo de referencia y Precio base; Todas mantuvo Marca
y Descripción. La policy se restauró explícitamente a Descripción Opcional,
Marca Esencial y Costo de referencia Esencial, quedando en `v6`. Chrome pasó
escritorio, `768 px` y `640 px` en claro/oscuro, sin overflow horizontal; Tab
y Shift+Tab conservaron el orden entre celdas. No se guardó carga, no se
ejecutó Analyze/Apply y PostgreSQL confirmó `45` CatalogItems y `77`
SupplierCatalogVersions antes y después. La suite material PBI-041 pasó con
74 migraciones. Es un checkpoint local para Owner Review, no aceptación,
integración ni deploy.

### UX-003.3A — Composer column resize freeze regression

La validación posterior de UX-003.3 encontró una regresión de presentación
reproducible: al arrastrar un separador, el navegador podía producir un ancho
fraccional. Ese valor se guardaba en `sessionStorage` y llegaba sin normalizar
al template de columnas. El adaptador del grid acepta únicamente píxeles
enteros; lanzaba `RangeError` al adjuntar el ref y, como la raíz no tiene un
error boundary, React desmontaba el Composer y dejaba una pantalla blanca.

La remediación conserva un mapa de anchos por clave canónica de campo,
normaliza hidratación, arrastre, autoajuste y teclado a enteros dentro de los
límites gobernados, y descarta claves obsoletas o valores no finitos. Cambiar
entre `Esenciales` y `Todas`, o variar la policy efectiva, sólo cambia las
columnas visibles: no reconstruye ni corrompe el estado de ancho de los campos
canónicos. Chrome pasó arrastres repetidos, barra lateral abierta/colapsada,
vista poblada, escritorio, `768 px`, `640 px`, claro/oscuro y teclado. No se
guardó ni analizó una lista y no hubo mutación de `CatalogItem`,
`SupplierCatalogVersion`, policy, API, backend o PostgreSQL. Es un checkpoint
local para Owner Review, no aceptación, integración ni deploy.

### UX-003.4 — Required effective value enforcement

La policy `REQUIRED` del Tenant se comprueba ahora en servidor contra el valor
efectivo resultante, no contra una celda cruda de proveedor. Un dato explícito
o el valor existente de un `CatalogItem` resuelto de forma segura puede
satisfacerlo sólo cuando la operación lo conserva; títulos, historial y
defaults de SupplierSource nunca se infieren. Los mínimos FULL de dominio se
conservan y `ESSENTIAL`/`OPTIONAL` no bloquean.

Analyze persiste la razón tipada `MISSING_REQUIRED_EFFECTIVE_VALUE:<field>` y
deja Apply no disponible mientras exista atención. La UI cuenta filas afectadas
y campos faltantes separadamente y dirige a `Completar datos faltantes`, sin
relleno automático. Las filas excluidas y duplicados descartados no bloquean.
Una decisión manual de identidad vuelve a evaluar policy; Apply la relee en su
transacción para impedir bypass directo o endurecimiento posterior a Analyze.
No hay mutación de Catalog, Resolution ni Supplier-memory antes de Apply.

QA local aislado pasó NEW sin Marca bloqueado, update COMPACT conocido que
conserva Marca, y Apply rechazado tras endurecer policy. Chrome verificó el
resumen, foco de corrección y reanálisis con Marca explícita; la policy quedó
restaurada en `v8`: Descripción Opcional, Marca Esencial y Costo Esencial.
Composer pasó 768/640 px, claro/oscuro y teclado sin overflow. Es evidencia
local para Owner Review; no hubo Apply QA, integración ni deploy.

### UX-005.6 — Published data integrity and pending Brand governance

UX-005.6 materializa una barrera hacia adelante: un item nuevo y activo de una
carga `FULL` no puede quedar READY/APPLIED con precio base efectivo `0`; Analyze
expone atención tipada y Apply revalida dentro de la transacción. Costo cero
permanece permitido salvo policy `REQUIRED`. La gobernanza de Brand pendiente
usa `catalog.configuration.read/manage`, agrupa por identidad normalizada
Tenant-wide, reutiliza sólo coincidencias canónicas exactas y religa el grupo
atómicamente sin reescribir Supplier history. No se realizó ninguna promoción
de AviCell v3 ni se modificó la fila 411: ambas decisiones materiales quedan
para Owner. Véase [UX-005.6](../../domain/PRICE_LIST_UX_0056_PUBLISHED_DATA_INTEGRITY_AND_PENDING_REFERENCE_GOVERNANCE.md).

### UX-005.6A — Pending Brand display canonicalization

UX-005.6A conserva la gobernanza de UX-005.6 pero corrige únicamente la
presentación de los grupos pending: `raw observed != normalized key != display
proposal != canonical Brand`. El helper compartido da preferencia al canon
exacto existente; sin canon, ajusta sólo casing uniforme, conserva acrónimos
cortos y casing mixto, y nunca infiere una Brand por similitud. La propuesta de
promoción inicia con el display derivado, mientras raw, agrupación,
autorización, Catalog, Memory y Resolution siguen intactos hasta una decisión
explícita. AviCell permanece read-only: 16 grupos / 618 items no se promueven
ni se religan. Véase
[UX-005.6A](../../domain/PRICE_LIST_UX_0056A_PENDING_BRAND_DISPLAY_CANONICALIZATION.md).

### UX-004 — Catalog operational authorization audit

La auditoría UX-004 confirma que el modelo vigente sigue siendo
`Role -> capabilities -> autorización contextual` y que los endpoints de
Catalog son autoridad server-side. UX-004.1 materializa su fundación mínima:
`catalog.items.create`, `.update`, `.deactivate` y `catalog.import.read` se
registran, proyectan a sesión y se respaldan con migración reversible. El
backfill es estrictamente por capability previa: `catalog.manage` recibe sólo
los tres sucesores de item y `catalog.import.prepare` recibe sólo
`catalog.import.read`; `price_list.read` y roles sin Catalog no reciben nada.

Las operaciones individuales aceptan la capability explícita o el fallback
temporal y documentado de `catalog.manage`; alta conserva además
`catalog.prices.manage` y costo cuando corresponda. Historial masivo acepta
`catalog.import.read` o prepare transitorio, pero prepare continúa requiriendo
exactamente `catalog.import.prepare`. No se rediseñó la UX ni se modificaron
CatalogItem, SupplierCatalogVersion, Apply, policy, controles ADR-013 ni la
deuda previa de hard delete Category/Brand.

Véase [UX-004 Catalog Operational Authorization Audit](../../domain/PRICE_LIST_UX_004_CATALOG_OPERATIONAL_AUTHORIZATION_AUDIT.md).
El resultado es un checkpoint local para Owner Review; no crea una matriz de
roles nueva ni autoriza push, PR, integración o deploy.

### UX-004.2 — Price List + Item authority integration

La integración local de UX-004.2 conserva `price_list.read` como la lectura
ordinaria de lista, filtros, precios y detalle comercial seguro. Ese detalle
ya no exige `catalog.manage` y no contiene costo de referencia. La interfaz y
el servidor separan explícitamente `catalog.items.create` (compuesto con
`catalog.prices.manage` porque el precio base es obligatorio),
`catalog.items.update`, `catalog.items.deactivate`, precio, costo, override de
Branch y retiro masivo. Un lector ve **Ver detalle**, no controles mutantes;
una ruta directa de alta exige la misma composición y una ruta directa de
detalle vuelve a exigir `price_list.read`.

`catalog.manage` sigue como fallback servidor temporal de operaciones de item
para compatibilidad UX-004.1 y para la administración independiente de
Category/Brand. No se rediseñó el acceso de Bulk Composer, no se cambió
Category/Brand delete, ni se modificaron CatalogItem, SupplierCatalogVersion,
policy, Apply o datos de Owner. La evidencia local sigue en curso hasta que
concluyan las pruebas PostgreSQL y visuales autorizadas; no autoriza acciones
remotas.

### UX-004.3 — Bulk Read / Prepare / Apply separation

El Composer ahora admite tres autoridades independientes. `catalog.import.read`
abre historia, Source/Version, lifecycle, coverage, comparación y resultado
en modo **Solo lectura**, sin recursos ni controles de preparación.
`catalog.import.prepare` conserva Source/Draft, paste, Save, Analyze,
reconciliación, decisiones y purge de borradores, pero no muestra Apply.
`catalog.import.publish` habilita Apply de un batch `READY` sólo junto con las
capabilities de efecto presentes: creación, actualización, reactivación,
precio y costo. No hay verificación por nombre de rol ni lock
prepared-by/applied-by; la segregación es posible, no obligatoria.

El material PostgreSQL desechable prueba A prepara → READY y B publica una vez;
el audit de Apply conserva B como actor. Los contratos directos prueban que
read no crea/edita/analiza/decide/publica, prepare no publica y publisher no
prepara. Supplier delete nivel 2, bulk retire, policy, coverage, duplicates y
matching permanecen sin cambio. Es evidencia local para Owner Review: no hubo
push, PR, merge ni deploy.

### UX-004.4 — Role matrix and end-to-end authorization proof

UX-004.4 verifica la frontera ya materializada sin crear una matriz durable de
roles del Owner. Las fixtures PostgreSQL desechables asignan capacidades por
rol a perfiles Atención, Encargado, Publisher y Cost Viewer: la unión multirol
se resuelve desde asignaciones activas; una Branch o Tenant distintos no
reciben capabilities; y retirar `catalog.import.prepare` se observa en la
siguiente resolución protegida. Los contratos de operación prueban que la
lectura ordinaria no muta ni ve costo, preparación no publica, Publisher no
prepara y costo se mantiene separado de gestión.

La sesión vuelve a revalidarse tras mutaciones administrativas y cada endpoint
resuelve capabilities en servidor. Chrome local revisó el editor existente en
escritorio, `768 px` y `640 px`, claro/oscuro, Tab y Shift+Tab, sin overflow;
se corrigió una única etiqueta humana faltante para `catalog.suppliers.delete`.
No se guardó un cambio de rol, no se usó PIN, ni se mutaron CatalogItems,
SupplierCatalogVersions o datos del Owner. El resultado queda listo para Owner
Acceptance; no autoriza integración, push, PR, merge ni deploy.

## Problema

El Owner recibe listas variables de proveedores, las limpia y calcula precios
en Google Sheets y necesita incorporar cientos o miles de filas sin crear
duplicados ni sobrescribir identidad, clasificación, precio o costo por una
coincidencia débil. Los proveedores pueden no entregar SKU, barcode o código
estable; repetir alta manual no escala y un import CRUD no conserva qué se
recibió, qué se decidió ni cómo corregirlo.

## Outcome

El Owner pega una lista en un Composer durable, conserva una versión inmutable
del catálogo del proveedor, reconcilia cada observación contra Catalog y aplica
atómicamente un diff revisable. Una segunda versión reconoce mappings históricos
exactos donde son únicos y consistentes, sin publicar silenciosamente ni cambiar
la identidad estable del `CatalogItem`.

```text
Google Sheets -> paste Composer -> SupplierCatalogVersion/Listing
  -> match exacto o reconciliación humana -> preview/diff
  -> confirmación Owner -> CatalogUpdateBatch atómico -> reporte
```

## Decisiones Owner aplicables

`OD-BI-001..010` están aprobadas y promovidas en
[Price List Architecture](../../architecture/PRICE_LIST_ARCHITECTURE.md) y el
[discovery](../../domain/PRICE_LIST_BULK_IMPORT_AUDIT_AND_DOMAIN_DESIGN.md).
En síntesis: Composer first; source/version/listing/mapping; código de proveedor
opcional; casing preservado; precio/costo proponibles en matches confiables;
cambios descriptivos/clasificación opt-in; pending deliberada permitida;
unresolved bloquea; sin bulk cost revoke; raw 90 días; subset estructurado
permanente; mappings exactos preseleccionables; reconciliación avanzada separada.

### Owner decisions — trusted history and bounded candidates

El Owner aprobó `CM-001..CM-009` el 2026-09-15 conforme a la
[auditoría focalizada](../../domain/PRICE_LIST_CANDIDATE_MATCHING_AND_TRUSTED_HISTORY_AUDIT.md):

- historia exacta, única, consistente, publicada y compatible puede resolver
  automáticamente identidad para `UNCHANGED`, `UPDATE` y `REACTIVATE`;
- auto-resolve nunca publica: `Aplicar lote` continúa explícito y obligatorio;
- después de un miss exacto, `CANDIDATE_MATCH` sólo descubre, ordena y explica
  candidatos; no crea mapping ni cambia Catalog;
- una elección Owner se vuelve memoria exacta reutilizable únicamente después
  de un publish exitoso;
- la UX prioriza `Requieren atención` y conserva `Resueltas` inspeccionable;
- transformaciones grupales reutilizables, aliases, stopwords y fuzzy writes
  permanecen diferidos a un PBI posterior.

### Owner iteration — canonical title and observed-title history

El hallazgo Owner del 2026-09-16 separa tres conceptos que no pueden colapsarse:

- `CatalogItem.itemId` es identidad estable y es la referencia futura de
  Inventory, Caja y Repairs;
- `CatalogItem.title` es el título canónico vigente y puede cambiar sin crear
  otra identidad;
- `SupplierListing.supplier_title` es la observación exacta, Source/Version
  scoped e inmutable; no es un alias global.

Al confirmar `Mismo artículo` con un título distinto, el Owner elige
explícitamente `KEEP_CURRENT` —default seguro— o `ADOPT_OBSERVED`. La decisión
es provisional y el rename sólo ocurre dentro del mismo Apply Batch atómico.
Una falla, exclusión o cancelación no renombra ni aprende. Tras publish, todos
los títulos observados publicados para ese `itemId` participan en búsqueda
Tenant-scoped; el resultado sigue deduplicado, filtrable y paginado por
`CatalogItem`.

### Owner remediation — RowDecision error JSON shape

La revisión local del 2026-09-16 detectó que una decisión manual podía escribir
un arreglo JavaScript directamente en la columna JSONB `errors`; el driver lo
materializaba como `{}`. El contrato permanece estricto en todo el recorrido:
`RowDecision.errors` siempre se lee y se entrega como `string[]`.

La remediación concentra la normalización de datos históricos en dominio,
serializa explícitamente el JSON en create/analyze/decide y mantiene una
defensa de render en Composer. No hay migración ni backfill: las filas
históricas malformadas se leen como `[]` y la siguiente decisión las normaliza
al escribir. La operación sigue siendo sólo de reconciliación: no crea ni
modifica `CatalogItem`, Resolution, Memory, audit ni Apply/Publish.

### Owner iteration — Exception-first reconciliation

La reconciliación es **exception-first**: una fila resuelta ya tiene identidad
suficientemente determinada para continuar y no exige confirmación repetida
del Owner. Cuando no quedan decisiones pendientes, Composer abre la vista
`Requieren atención` con el estado compacto `Todo resuelto`; `Resueltas` y
`Todas` continúan disponibles únicamente para auditoría.

Las acciones masivas se muestran sólo cuando existe una fila no resuelta a la
que realmente puedan aplicarse. Una fila `APPLY` no ofrece `Incluir`; su única
acción excepcional discreta es excluirse del lote. Una fila `EXCLUDE` comunica
su estado y ofrece sólo `Volver a incluir`. La elección KEEP_CURRENT /
ADOPT_OBSERVED conserva su semántica y aparece como `Cambiar nombre` sólo
cuando el título observado difiere del canónico.

Los códigos diagnósticos permanecen en DTO, logs, pruebas y evidencia, pero no
son parte de la superficie normal para el Owner: la UI muestra explicaciones
humanas cuando corresponden. Este cambio no altera matching, scoring, historia
confiable, semántica de título canónico, cobertura COMPLETE/PARTIAL,
persistencia ni Apply/Publish.

### Owner iteration — explicit completeness and applied results

`PARTIAL` y `COMPLETE` son declaraciones obligatorias de cada versión del
proveedor; no existe un default implícito en las fronteras Create o Replace.
El Composer inicia intencionalmente en `PARTIAL`, pero transmite siempre la
declaración explícita. Un request ausente, nulo o fuera del conjunto cerrado se
rechaza antes de persistir. La misma declaración queda inmutable al analizar y
aplicar, y las respuestas de versión incompletas se rechazan en el cliente
antes de que la UI pueda inferir una semántica distinta.

El resultado visual distingue dos estados: antes de Apply, `Todo resuelto`
significa que las filas están listas para aplicar; después de Apply, el estado
histórico dice `Lote aplicado`, informa cuántas filas fueron procesadas y
resume únicamente categorías reales no nulas. Ninguno de esos textos cambia
matching, cobertura, ausencia, atomicidad ni la historia de una versión ya
aplicada.

### Owner authorization — complete baseline plausibility and coverage explainability

Las reglas `BA-001..BA-004` quedan aceptadas para este alcance de PBI-041:

- **BA-001:** una baseline se deriva automáticamente de la Version
  `COMPLETE/APPLIED` anterior más reciente del mismo Tenant y SupplierSource;
  no se promueve manualmente ninguna Version;
- **BA-002:** antes de Apply, una `COMPLETE` con baseline de al menos 20 filas
  que conserva 25% o menos exige reconocimiento explícito del actor. La regla
  es una advertencia determinista y auditable, no un bloqueo permanente;
- **BA-003:** la cobertura explica, frente a baseline, filas continuadas, no
  observadas y adicionales. “Adicionales” no se llama ni decide como `NEW`;
- **BA-004:** la ausencia es sólo observacional y nunca altera lifecycle,
  identidad, títulos, identifiers, revisiones, Resolution ni Memory de
  Catalog.

Un crecimiento o una reducción normal no requiere reconocimiento. `PARTIAL`
no calcula cobertura ni plausibilidad. La confirmación se mantiene dentro del
request de Apply y su audit, por lo que un retry no puede sortearla ni duplicar
efectos.

### Owner iteration — operational coverage and applied-result clarity

`UXP-001..005` refinan la superficie de revisión sin cambiar matching,
baseline, completeness, ausencia, Apply ni retiro. La cobertura conserva
`CONTINUED`, `NOT_OBSERVED` y `ADDITIONAL`, pero prioriza los dos cambios
excepcionales y deja las continuaciones como auditoría bajo demanda. Un cero
conserva la métrica, no un CTA inútil.

Cada detalle expone relación de coverage, relación conocida con Catalog,
lifecycle actual y, cuando existe, provenance inmutable de `Resolution`. Antes
de Apply una fila adicional `NEW` comunica que se creará; después de Apply, la
proyección usa la Resolution publicada para distinguir creación, match o
reactivación sin inferir un evento. Esto corrige una `STALE_PROJECTION`:
`target_item_id` de una decisión `NEW` permanece correctamente nulo, pero no
debe ser la única fuente tras publish.

El resultado aplicado conserva prioridad. El retiro `CREATED`-only conserva
capability, PIN Level 2 y todos sus controles, pero se ubica como acción
secundaria del lote. La comparación histórica conserva su comportamiento y se
abre sólo mediante un disclosure accesible; la cobertura no llama “Nuevo” a un
adicional.

## Owner iteration — safe Catalog retirement

El Owner aprobó `OD-RESET-001..005` el 2026-09-14:

- `Vaciar lista de precios` inactiva todos los `CatalogItem` activos del Tenant;
  nunca hace hard delete y conserva identidad, identificadores, revisiones,
  Supplier history, mappings, memoria, batches y audit;
- la acción es nivel 2 de ADR-013 y exige la capability dedicada
  `catalog.items.bulk_retire`, plan server-side, PIN del mismo actor,
  confirmación exacta y revalidación transaccional fail-closed;
- un batch aplicado sólo ofrece `Retirar artículos creados por este lote`, con
  targets demostrados por Resolution `CREATED`; `MATCHED` y `UPDATED` no se
  revierten;
- `ACTIVE CATALOG EMPTY` no equivale a `NO HISTORICAL CATALOG MEMORY`; un item
  histórico retirado con mapping exacto, único y compatible se clasifica
  `REACTIVATE`, conserva identidad y nunca reaparece como `NEW`;
- el subtítulo de fila usa `Original:`; Supplier Source conserva su identidad
  propia visible.

El lifecycle `ACTIVE/INACTIVE` ya existente representa el retiro sin cambiar
schema de `CatalogItem`. Las migraciones nuevas sólo registran la capability,
el plan autoritativo y la evidencia append-only del retiro.

## Owner iteration — historical Catalog reactivation

El hallazgo Owner posterior exige que una versión nueva de proveedor pueda
reconocer como la misma identidad un `CatalogItem INACTIVE` cuando la memoria
histórica es exacta, única, consistente, Tenant-scoped y compatible con Tipo.
Esa fila se clasifica `REACTIVATE`: nunca `NEW`, nunca requiere UUID manual y
nunca deriva de similitud o título. El mismo publish atómico cambia
`INACTIVE→ACTIVE` y aplica los diffs permitidos de metadata, precio y costo.

La auditoría de persistencia confirmó que el lifecycle existente ya representa
la transición; la única expansión física necesaria es admitir `REACTIVATE` en
el CHECK cerrado de `CatalogUpdateRowDecision.classification`. No se creó una
segunda identidad ni una variante `REACTIVATE_AND_UPDATE`.

## Owner iteration — Supplier history, automatic versioning and governed delete

La UI distingue el catálogo oficial (`Lista`) del historial de intake
(`Fuentes y versiones`). Crear un proveedor ya no crea una versión vacía:
seleccionar la Source e iniciar una carga sólo prepara el workspace; el servidor
crea la Version al primer guardado válido.

Cada SupplierSource tiene una secuencia monotónica Tenant-scoped. El backend
asigna `v1`, `v2`, ... con lock de Source, idempotencia y unique físico. Fecha,
filename y descripción no forman la identidad; varias versiones del mismo día
son válidas y los números consumidos no se reutilizan. Una descripción opcional
permanece como contexto histórico.

`catalog.suppliers.delete` es una capability explícita y asignable. Su operación
ADR-013 nivel 2 sólo elimina una Source si todas sus Versions son `DRAFT` y no
existen Resolution, ReconciliationMemory, RetirementPlan ni RetirementEvent.
La UI exige dos confirmaciones y PIN del mismo actor; el backend revalida
Tenant/Session/Station/User/capability/versión/dependencias dentro de la
transacción. RowDecisions, Listings, draft Batch, raw, Versions y Source se
eliminan; `CatalogItem`, identificadores, revisiones y evidencia publicada
nunca se tocan. Un evento append-only desacoplado conserva la eliminación.

La regla transversal queda promovida: todo hard delete necesita capability
explícita; una capability amplia de administración no es sustituto. La auditoría
encontró deuda previa en Category/Brand de Catalog (`catalog.manage`) y en
referencias de Repairs (`repairs.catalogs.manage`); PBI-041 la documenta pero no
modifica otros módulos.

### Auditoría UX-001 — Supplier Selection Gate

La auditoría de flujo de operador identificó que navegar Source/Version y
empezar una carga nueva comparten una selección implícita de proveedor. El
Owner aceptó UX-001-D01..D10 y la implementación local separa browse de la
propiedad pendiente de una carga. El detalle de riesgo, decisiones y prueba
local está en
[UX-001 — Supplier Selection Gate Audit](../../domain/PRICE_LIST_UX_001_SUPPLIER_SELECTION_GATE_AUDIT.md).

El gate sólo aparece para una carga nueva: nunca hereda la Source visitada ni
la primera Source devuelta por API. Elegir una Source abre el workspace sin
Version vacía; antes del primer guardado se puede cambiar con protección de
trabajo material. El primer `POST` sigue siendo la frontera que fija
`source_id` y recibe `vN` del servidor. La creación contextual de proveedor
vuelve a la nueva carga, mientras que la creación independiente conserva el
trabajo local existente. Defaults por proveedor siguen diferidos.

### Owner iteration — UX-001B workspace entry and recoverable navigation

El Owner aceptó `UX-001B-D01..D07` para aclarar la jerarquía del Composer sin
cambiar su dominio: `Fuentes y versiones` es navegación y el lado derecho es
workspace sólo después de seleccionar una Version o una Source para una carga
nueva. Existe un único CTA primario `Nueva carga` en la navegación; el estado
sin selección es neutro y no duplica un CTA ni simula loading.

El panel de Sources permanece colapsable, pero el control Mostrar/Ocultar vive
en el shell persistente del Composer, fuera de ese panel. Conserva nombre
accesible, tooltip, foco visible, teclado y estado de presentación durante
empty, nueva carga y Version histórica/persistida. No se guarda como preferencia
ni se reabre automáticamente al cambiar de navegación. El alcance no incluye
defaults, autosave, Analyze, matching, coverage, reconciliación, Apply,
persistence ni migraciones.

### Owner iteration — UX-001C Sources panel collapse affordance polish

UX-001C reemplaza únicamente la apariencia de ese control: con Sources abierto
se integra como control terciario direccional en el encabezado `Fuentes y
versiones`; con Sources cerrado se conserva fuera del contenido oculto como un
botón compacto integrado al borde del workspace, sin rail ni columna de grilla
residual. Mantiene nombres accesibles
explícitos, foco/teclado y la recuperación de panel. No modifica `sourcesOpen`,
Source/Version seleccionada, proveedor pendiente, gate, fetch, dominio, API,
persistence ni migraciones.

El micro-polish Owner posterior alinea la manija cerrada con el contenido del
workspace. En el estado neutro, un tramo corto de borde la une visualmente al
borde izquierdo; en cargas y versiones se apoya en la primera tarjeta. Su
tratamiento en reposo es neutral y el foco visible conserva el token accesible.
El encabezado y control del estado abierto permanecen aprobados e intactos.

### Owner audit — UX-002 happy-path friction

UX-002 es una auditoría documental, no una implementación. Traza las
fronteras actuales entre declaración de completitud, borrador durable,
reconciliación y publicación para identificar qué pasos son mecánicos y cuáles
requieren decisión semántica o de gobierno. Conserva explícitos Source,
`PARTIAL`/`COMPLETE`, excepciones y Apply; no autoriza automatización silenciosa
ni cambios de producto. El análisis y las decisiones propuestas están en
[UX-002 — Bulk Catalog Happy-Path Friction Audit](../../domain/PRICE_LIST_UX_002_HAPPY_PATH_FRICTION_AUDIT.md).

### Local implementation — UX-002A Review List orchestration

The approved UX2-002/UX2-003 slice is implemented locally for Owner Review.
`Revisar lista` is the primary deliberate action for a new or modified local
draft: it validates, persists the existing durable snapshot once, and analyzes
the authoritative response from that Save. A Save failure never triggers
Analyze; an Analyze failure preserves the successfully saved draft for
recovery. `Guardar borrador` remains secondary, `Reanalizar versión` remains
available for an already analyzed unpublished version, and `Aplicar lote`
remains explicit. No domain, persistence, API, migration, PARTIAL/COMPLETE,
capture-mode, context/default, identity, or Catalog-publication semantics were
changed. See the UX-002 implementation record for local proof and boundaries.

### Owner audit — UX-002A.1 Duplicate input resolution

UX-002A.1 is a read-only audit of duplicate observations within one supplier
snapshot. It preserves AG `v60` exactly and separates a real identity conflict
from a known identity with contradictory values in the same snapshot. The
current implementation safely blocks Apply, but its generic UUID correction
control is not semantically appropriate when trusted history already resolves
both rows to the same CatalogItem. The audit records the exact trace,
invariants and Owner decisions `DUP-001` through `DUP-008`; it authorizes no
product behavior. See
[UX-002A.1 — Duplicate Input Resolution Audit](../../domain/PRICE_LIST_UX_002A1_DUPLICATE_INPUT_RESOLUTION_AUDIT.md).

### Local implementation — UX-002A.2 Duplicate input resolution

The approved DUP-1/DUP-3 slice is implemented locally. Exact same-identity,
same-value physical rows consolidate to one deterministic effective row while
the remaining row is traceably excluded. Same-identity rows with material value
differences stop as `DUPLICATE_VALUE_CONTRADICTION`; they compare by physical
row and a selected known row excludes its siblings before Apply and remains
stable through reanalysis. UUID mapping remains for genuine identity ambiguity
and is suppressed for known duplicate identity. No migration, change to
PARTIAL/COMPLETE, baseline authority, Memory/Resolution semantics or AG `v60`
mutation was made. UX-002B is tracked separately and does not alter those
 duplicate-resolution semantics.

### Local implementation — UX-002A.3 Duplicate resolution card

The contradictory same-snapshot path now presents one grouped Owner decision,
not independent generic conflict rows. The presentation derives its group only
from the authoritative duplicate contradiction reason and existing supplier
identity key. It foregrounds only values that differ, keeps per-row details
behind an accessible disclosure, and changes its local count to decision units.
Choosing a row retains the existing backend behavior and renders a compact
resolved card from the returned state. No domain, database, API, Apply,
Resolution, Memory, Coverage, COMPLETE/PARTIAL or matching behavior changed;
true identity conflict retains its UUID mapping path.

### Local implementation — UX-002B.1 Two-panel New Load modal

The existing New Load gate is now a wide, two-panel dialog: supplier search,
selection and contextual source creation live on the left; the explicit
`PARTIAL` / `COMPLETE` declaration lives on the right. The global footer keeps
Cancel and Continue together. A fresh gate starts with neither choice; Continue
is disabled until the operator explicitly selects both a SupplierSource and an
intent. The existing Change path keeps its already selected Source and intent
until save, and contextual source creation returns with the new Source selected
but no inferred intent.

This is presentation and interaction layout only. It reuses the established
`PARTIAL` / `COMPLETE` values, the existing focus-trapped Dialog, source and
intent guards, and the existing server-side authority. There is no change to
domain, API, persistence, migrations, analysis, coverage, matching, Apply or
Catalog behavior. Focused contracts, typecheck, production build and
architecture checks pass at commits `794a3ba` and `e766a99`. Authenticated
Chrome proof passed on 2026-09-17: the fresh blank gate kept Continue disabled,
both explicit intent cards enabled it only with AG selected, pre-save Change
preserved AG plus `COMPLETE`, contextual creation cancelled back to the gate,
and Escape/Tab/Shift+Tab preserved the established dialog behavior. The modal
was visually checked in light desktop, light 768/640 and dark 640 without
horizontal overflow or durable writes. This is ready for Owner Review, not
Owner acceptance.

## Alcance

### Experiencia Composer

- grid tabular virtualizado con paste de celda, columna y bloque rectangular;
- edición directa, selección, filas de draft, undo pre-publish, Tab/Shift+Tab,
  flechas, Enter, errores por celda, estado por fila y foco/anuncios accesibles;
- pegado natural `Título | Costo | Precio`: sólo se eliminan filas totalmente
  vacías al final del rango copiado; vacíos internos conservan su posición;
- `Completar datos faltantes` para Tipo, Category y Brand: helper secundario,
  explícito y cerrado por defecto que sólo completa campos vacíos; cada fila
  conserva sus valores y toda combinación sigue la aplicabilidad gobernada
  existente;
- vista esencial de tres columnas sin retirar el modelo completo; copy-fill
  por handle con undo, navegación de hoja de cálculo y columnas ajustables por
  arrastre o auto-fit acotado;
- workspace grid-first con panel de fuentes y contexto del lote compactables,
  toolbar integrada, header de columnas estable y regreso reusable a Lista de
  precios, sin duplicar el banner de ambiente que ya pertenece al shell;
- estado de presentación result-first: durante captura el Draft abre la grid;
  después de análisis exitoso y al abrir una Version `INGESTED` el resultado
  abre con la grid colapsada. Mostrar/Ocultar lista no persiste ni altera filas,
  batch, decisiones, cobertura, reconciliación o `CatalogItem`; un error de
  celda vuelve a expandir la grid antes de enfocarlo;
- validación exhaustiva previa al guardado con issues estructurados de lote o
  celda, orden determinista, contador anterior/siguiente, foco y scroll a cada
  error —incluidas filas virtualizadas— y banner persistente sólo para fallos
  globales o desconocidos;
- feedback transitorio no bloqueante mediante Toast; los errores accionables
  permanecen asociados al control o celda que debe corregirse;
- draft durable/versionado, alta completa, actualización compacta y creación de
  una Supplier Catalog Version;
- hard caps: 10,000 filas, 32 columnas, 200,000 celdas no vacías, 4,096
  caracteres/celda y 10 MiB UTF-8; rechazo completo, sin truncar; 10,000 sujeto
  a benchmark y 50,000 sólo caracterización;
- la grid conserva virtualización y trabajo acotado aunque el Draft alcance el
  límite gobernado.

### Evidencia y reconciliación

- `SupplierSource` mínimo Tenant-scoped;
- `SupplierCatalogVersion` y `SupplierListing` inmutables después de `INGESTED`;
- content hash, schema/signature version, label/revision y lineage correctiva;
- supplier code opcional; `SupplierObservedTitle` exacto separado del título
  normalizado/editable propuesto, costo/moneda observados y hints/tags;
- resolución manual y mapping persistente/corregible;
- preselección de mappings históricos exactos, únicos y consistentes visible en
  preview; confirmación del batch siempre requerida;
- contraste textual bounded no es conflicto de identidad: sin evidencia
  durable, un candidate débil puede descartarse como `NEW`; sólo identifiers,
  memoria durable, tipo/referencia o invariantes incompatibles producen
  `CONFLICT`;
- pending Category/Brand mediante el governance existente de Catalog;
- comparación por SupplierSource: una carga `PARTIAL` no expresa ausencia; una
  carga `COMPLETE` puede mostrar `NO OBSERVADO EN ESTA VERSIÓN COMPLETA` sólo
  contra una `COMPLETE` anterior del mismo Tenant/Source, sin mutación de
  lifecycle por ausencia;
- la cobertura es información de primera clase posterior al análisis: la
  reconciliación trata las filas recibidas y la cobertura informa qué no fue
  observado en una `COMPLETE`. No se mezclan como `RowDecision` ni crean un
  flujo de retiro. `PARTIAL` significa ausencia `NOT EVALUATED`; `COMPLETE`
  con baseline significa `NOT OBSERVED`; `NOT OBSERVED` no significa
  `INACTIVE`.

### Aplicación a Catalog

- preview observation/proposal y before/after por campo;
- `CatalogUpdateBatch` y `CatalogUpdateRowDecision` separados de la versión de
  proveedor;
- creación de CatalogItem con SKU/barcode generados server-side cuando faltan;
- precio base y Reference Cost importado como revisiones append-only sólo ante
  cambio real;
- idempotencia, optimistic concurrency, audit/correlation y reporte por fila;
- publish all-or-nothing para filas incluidas y revalidación completa dentro de
  una única transacción/conexión PostgreSQL.

### Retención

- 90 días: raw completo de clipboard/adaptador, celdas no mapeadas, artefactos
  temporales y diagnóstico detallado;
- permanente: metadata Source/Version, listing estructurado esencial, firma,
  resolution/mapping, diffs/revision IDs y audit;
- cleanup idempotente y observable que elimina sólo raw vencido;
- costos observados omitidos server-side sin capability.

## Matching permitido

Señales fuertes: `itemId` confiable cuando aplica, SKU interno, barcode interno,
supplier code cuando existe y observación/firma histórica exacta. Una
preselección histórica requiere mismo Tenant/Source, una única historia
consistente hacia el mismo item y cero contradicción de identifier o Tipo. Si
está inactivo, la clasificación explícita es `REACTIVATE`; ambigüedad o
incompatibilidad sigue bloqueando.

Título/estructura probable y cualquier similitud sólo crean candidatos humanos.
No existe fuzzy write, title-only update, last-row-wins ni mapping oculto.

## Semántica de campos

| Campo existente | Regla |
|---|---|
| Tipo | inmutable; contradicción bloquea |
| SKU/barcode | match-only e inmutables por bulk |
| título | `SupplierObservedTitle` conserva el original exacto; el título propuesto es editable y nunca renombra Catalog automáticamente |
| descripción | diff + opt-in explícito |
| Category/Brand | diff compatible + decisión explícita/pending gobernada |
| precio base | revisión append-only sólo en cambio real |
| Reference Cost | revisión `IMPORTED` sólo en cambio real y con permisos |
| lifecycle | sólo `INACTIVE→ACTIVE` para `REACTIVATE` histórico único; ningún otro cambio bulk |
| Branch override | intacto |

Celda vacía en update significa sin cambio; `0` es valor explícito. No existe
revocación masiva de costo. `SupplierObservedCost`, `ReferenceCostRevision` y
futuro `ProcurementPurchaseCost` nunca se colapsan.

## Lifecycles

```text
SupplierCatalogVersion:
DRAFT -> INGESTING -> INGESTED
DRAFT/INGESTING -> CANCELLED | FAILED
INGESTED --corrección nueva--> SUPERSEDED derivado

CatalogUpdateBatch:
DRAFT -> ANALYZING -> RECONCILING -> READY -> COMMITTING -> COMPLETED
DRAFT/ANALYZING/RECONCILING/READY -> CANCELLED
ANALYZING/COMMITTING -> FAILED
COMMITTING --stale--> RECONCILING

CatalogRetirementPlan:
PENDING -> EXECUTED
PENDING -> STALE | EXPIRED
```

Nada antes de `COMMITTING` escribe Catalog. Una pending reference elegida de
forma deliberada puede publicar; `UNRESOLVED`, `AMBIGUOUS`, `CONFLICT`,
`INVALID` o `STALE` bloquean. Un error en cualquier fila incluida revierte el
batch completo.

## Autorización

- `catalog.import.prepare`: Composer, draft, Source/Version/Listing, análisis y
  reconciliación sin publicar;
- `catalog.import.publish`: confirmar el batch; no sustituye capabilities de
  cada campo;
- cambios de item requieren además `catalog.manage`;
- precio base requiere `catalog.prices.manage`;
- ingestar/ver/publicar costo requiere `catalog.reference_cost.read` y
  `catalog.reference_cost.manage` según la operación.

Tenant/Branch/Station/User/Session se derivan del contexto confiable. Branch se
conserva para atribución; el target es Tenant-wide. Capability y Session se
revalidan al commit. Publicación permanece nivel 1 de ADR-013 mientras mantenga
confirmación, reversibilidad por revisiones, idempotencia y audit.

El retiro masivo es nivel 2 de ADR-013. Requiere
`catalog.items.bulk_retire`, que no se sustituye por `catalog.manage`; plan
server-side ligado a Tenant/actor/Branch/Station/Session; PIN del mismo actor;
confirmación exacta; y revalidación de sesión, capability, contexto y conjunto
de items dentro de la transacción serializable. El control es de un uso.

## Performance budgets

Los budgets p95 autoritativos están en arquitectura y su evidencia es gate del
checkpoint: paste 1k/10k ≤1/3 s; análisis ≤5/30 s; primera preview ≤1.5/2 s;
publish total ≤8/30 s; transacción DB ≤5/15 s; heap adicional ≤100/250 MiB;
cleanup ≤10/60 s. Una tarea de main thread no excede 200 ms. 1,500 filas es la
demostración material; 10,000 sólo se anuncia si pasa todo; 50,000 caracteriza
y el producto inicial lo rechaza sin persistencia parcial.

## Criterios de aceptación

1. Un Owner crea SupplierSource mínimo y una primera versión sintética de
   aproximadamente 1,500 filas sin códigos obligatorios mediante paste.
2. El draft sobrevive reload y conserva versión optimista; dos Owners no pisan
   decisiones.
3. Nuevos CatalogItems reciben SKU y barcode server-side; duplicados/conflicts
   se explican sin last-row-wins.
4. Preview separa observation, propuesta, match/evidencia, before/after,
   pending deliberada, unresolved y Branch overrides preservados.
5. Publish se bloquea con unresolved/conflict/stale o capability revocada y no
   deja mutación parcial.
6. Publish exitoso produce items/revisiones/audit/reporte y retry devuelve el
   mismo outcome sin duplicar.
7. Una segunda versión preselecciona sólo mappings históricos exactos válidos;
   el Owner confirma el batch y no hace click por cada fila conocida.
8. Un mapping corregido conserva historia; una observación históricamente
   inconsistente queda ambiguous.
9. Título del proveedor no renombra Catalog; descripción/clasificación requieren
   opt-in; price/cost sólo crean revisión ante cambio.
10. Una fila desaparecida no cambia Catalog lifecycle; un override Branch sigue
    prevaleciendo después del cambio de base.
11. Dos Tenants no leen, infieren, relacionan ni mutan Sources, Versions,
    Listings, costs, mappings, batches o items ajenos.
12. Raw vencido se elimina sin afectar evidencia estructurada, mappings,
    revisiones ni reporte permanente.
13. Chrome demuestra grid/teclado/foco en 1280, 768 y 640, light/dark, y los
    budgets obligatorios quedan registrados sobre SHA exacto.
14. El caso Owner de 36 filas pega `Título | Costo | Precio` sin crear centenas
    de filas terminales vacías, conserva mayúsculas técnicas en la propuesta,
    mantiene el original del proveedor, permite fill/undo y recupera Draft y
    contexto tras reload.
15. Guardar un draft inválido encuentra todos los errores client-side sin
    persistencia parcial, lleva el foco al primero y permite recorrerlos en
    orden lote → fila → columna, incluso si la fila está fuera del DOM
    virtualizado.
16. El usuario no captura una revisión manual: el servidor asigna el siguiente
    `vN`; un fallo global o no clasificable conserva banner persistente. El
    Composer usa Toast sólo para confirmaciones transitorias.
17. `Vaciar lista de precios` deja cero `CatalogItem` activos sin eliminar
    identidad, SKU, barcode, revisiones, Supplier history, mappings, memoria,
    batches o audit.
18. Una nueva versión del Historical Tenant reconoce de forma exacta, única y
    compatible el target retirado, lo clasifica `REACTIVATE` y publica sobre el
    mismo itemId/SKU/barcode; un match ambiguo o incompatible sigue bloqueado.
19. `REACTIVATE` puede agregar en la misma transacción revisiones nuevas de
    precio/costo y metadata permitida; no existe `REACTIVATE_AND_UPDATE`, no hay
    writes parciales y un retry no duplica efectos.
20. `Retirar artículos creados por este lote` deriva exclusivamente targets con
    resolución `CREATED`; `MATCHED` y `UPDATED` permanecen intactos y la UI no
    afirma que el batch fue revertido.
21. Un Virgin Tenant sintético, aislado y reversible demuestra 36 filas AG
    `NEW` sin destruir historia del Tenant habitual.
22. Reautenticación con otro usuario, plan stale/expirado/contexto cambiado o
    capability revocada falla cerrada sin retiro parcial y deja evidencia de
    rechazo cuando la transacción puede registrarla de forma segura.
23. Guardar dos cargas concurrentes para la misma Source asigna números
    monotónicos únicos; un retry idempotente devuelve la misma Version.
24. Crear o seleccionar un proveedor no persiste una Version vacía; el primer
    guardado válido recibe el siguiente `vN` server-side y admite descripción.
25. El sidebar agrupa Provider → Versions newest-first con estado de producto,
    fecha, filas y descripción; al ocultarlo desaparece por completo y el grid
    ocupa el ancho liberado, con un único control accesible para reabrirlo.
26. Una Source sólo de draft puede eliminarse con
    `catalog.suppliers.delete`, reautenticación y dos confirmaciones; Enter en
    la primera y doble click no saltan ni duplican la operación.
27. Una Source con historia publicada o dependiente se bloquea server-side y
    ningún CatalogItem cambia; sin la capability la UI no ofrece la acción y el
    backend la rechaza.
28. La eliminación segura conserva un evento append-only con contexto y
    conteos; no usa cascade como decisión de dominio.
29. En cobertura COMPLETE, un adicional `NEW` explica su estado pre-Apply y,
    tras Apply, expone la provenance material de Resolution; los cambios
    excepcionales preceden a continuaciones, retiro y comparación accesibles.

## Owner checkpoint futuro

No hay aceptación por pruebas backend. Owner debe revisar lado a lado:

- Historical Tenant: catálogo activo en cero después del retiro, historia y
  mappings intactos, y nueva versión que muestra `REACTIVATE` con el mismo
  itemId y el diff before/after;
- Virgin Tenant aislado: 36 pantallas AG materialmente `NEW`, aplicables como
  first intake real;
- caso realista: 36 pantallas sintéticas pegadas desde una matriz equivalente a
  Google Sheets, con `Refacción / Pantallas / Apple`, costo y precio;
- el mismo caso con errores deliberados en filas cercanas y alejadas para
  revisar foco, scroll, mensajes por celda y navegación anterior/siguiente;
- Version 1: ~1,500 filas sintéticas/anonimizadas de proveedor informal sin
  códigos, resolución de mappings/nuevos/pending y publish;
- Version 2: títulos/costos modificados, con known/changed/new/ambiguous/
  disappeared; exact history preseleccionada y cambios débiles sin auto-write;
- Catalog sin duplicados, identidad estable, revisiones correctas, overrides
  intactos, reporte y reload.
- jerarquía `Proveedor → vN`, descripciones y varias versiones del mismo día;
  panel abierto/cerrado sin rail residual;
- eliminación doble confirmada de una Source sintética sólo-draft y bloqueo
  visible de AG/otras Sources con historia.

## Fuera de alcance

- systematic rename detection y asistencia automática `Display→Pantalla`;
- advanced tag classification, group transformation/resolution y comparación
  avanzada entre versiones;
- CSV/XLSX o supplier API adapters;
- Supplier maestro global, Procurement, compras/recepción/cuentas por pagar;
- Inventory, Caja, Repair Concepts, Pedidos o Solicitudes;
- bulk cost revoke, Branch override mutation, lifecycle masivo, business
  reversal avanzada, Files/R2, fórmulas/macros/worksheets.
- transformaciones automáticas de datos distintas del copy-fill literal
  aprobado para la grid.

El outcome diferido se llama **Advanced Supplier Reconciliation**. No tiene PBI
ID, no está seleccionado y no está Ready.

## Dependencias

- Contratos Catalog/Pricing materializados en `main` por PBI-040.
- El cierre de PBI-040 satisface sólo la dependencia precedente; selección e
  implementación de PBI-041 conservan gates y autorización Owner propios.
- ADR-004/012/013, DEC-005/049/051, política de migraciones y delivery vigentes.
- [Persistence Design](../../quality/evidence/pbi-041/PERSISTENCE_DESIGN.md),
  [Threat Model](../../quality/evidence/pbi-041/THREAT_MODEL.md) y
  [Test Strategy](../../quality/evidence/pbi-041/TEST_STRATEGY.md).

## Riesgos y controles

| Riesgo | Control de entrada |
|---|---|
| identidad equivocada por similitud | strong exact + preview + decisión humana; no fuzzy write |
| pérdida/fuga Tenant o costo | scope compuesto, capabilities y responses/logs redacted |
| publicación parcial/replay | única transacción, expectedVersion e idempotency |
| evidencia mutable o borrada | Version/Listing inmutables, correction lineage, no cascades |
| grid/DB exhaust | virtualización, hard cap y budgets medidos |
| raw retenido indefinidamente | `expiresAt`, cleanup idempotente y tests de no cascada |
| scope XL adelanta outcome avanzado | criterios y exclusiones explícitos; checkpoint Owner propio |

## Estrategia de entrega

Checkpoints internos del mismo WIP, no PBIs técnicos separados:

1. domain/persistence/security y fixtures deterministas;
2. Composer durable + Supplier Version/Listing;
3. matching/reconciliation/version comparison;
4. preview + atomic apply + report/cleanup;
5. verification de riesgo alto, budgets, Chrome y Owner checkpoint.

La implementación local y su commit están materializados. PR, merge, deploy,
Owner Acceptance y Done requieren autoridad y gates posteriores; este checkpoint
no los concede.

## Trazabilidad

- **Objetivo de producto:** actualizar Catalog en volumen sin perder identidad,
  historia, aislamiento o control humano.
- **Epic:** [EPIC-015](../EPICS.md#epic-015--catalog-and-pricing).
- **Decisiones:** `OD-BI-001..010`, `OD-RESET-001..005`,
  ADR-004/012/013 y DEC-005/049/051.
- [Price List Architecture](../../architecture/PRICE_LIST_ARCHITECTURE.md)
- [Bulk Composer Discovery](../../domain/PRICE_LIST_BULK_IMPORT_AUDIT_AND_DOMAIN_DESIGN.md)
- [Definition of Ready](../../quality/evidence/pbi-041/DEFINITION_OF_READY.md)
- [Threat Model](../../quality/evidence/pbi-041/THREAT_MODEL.md)
- [Test Strategy](../../quality/evidence/pbi-041/TEST_STRATEGY.md)
- [Persistence Design](../../quality/evidence/pbi-041/PERSISTENCE_DESIGN.md)
- [Implementation Evidence](../../quality/evidence/pbi-041/IMPLEMENTATION_EVIDENCE.md)
- **Candidato funcional:** working candidate en
  `feature/pbi-041-bulk-catalog-composer`; SHA final se registra después de
  congelar esta iteración.
- **PR/merge/deploy/release:** no realizados ni autorizados.
- **Revisor actual:** Owner para checkpoint funcional local.

## Próxima acción

Owner decide `UX3-001..017` de la auditoría
[UX-003 Bulk Catalog Field Policy](../../domain/PRICE_LIST_UX_003_BULK_CATALOG_FIELD_POLICY_AUDIT.md).
La auditoría no implementa un setting, API, migración, regla nueva de Catalog o
cambio de Composer: separa los mínimos actuales de FULL/COMPACT de una futura
calidad efectiva Tenant-owned. En particular, no autoriza exigir raw Marca en
actualizaciones COMPACT ni derivar valores desde Supplier history. Luego Owner
puede autorizar un slice explícito y acotado, o continuar la revisión de los
checkpoints locales existentes.

Owner revisa UX-002E.1 local: `Completar datos faltantes` sustituyó el bloque
primario de contexto. Es un disclosure cerrado por defecto, vacío en cada carga
nueva y sólo completa campos de Tipo, Categoría o Marca que estén vacíos tras
una acción explícita; no persiste preferencias, no infiere datos, no guarda ni
analiza. La evidencia v52/v53 sigue prohibiendo perfiles, sugerencias o
defaults automáticos para identidad. AG `v64` continúa como evidencia de la
falla previa y AG `v65`/`v66` como fixtures aislados resueltos sin Apply. No
iniciar gates de integración ni PBI-042.

### UX-002E — Batch context defaults friction audit

La auditoría read-only estableció la frontera de identidad. UX-002E.1 aplicó
sólo su consecuencia UI autorizada: Tipo, Categoría y Marca viven ahora como
ayuda efímera de la carga actual, cerrada y vacía al iniciar una carga nueva;
la acción sólo llena vacíos y el Listing conserva únicamente los valores finales
sin provenance. La evidencia v52/v53 confirma que Marca puede cambiar `NEW` por
`REACTIVATE`, por lo que ninguna sugerencia o perfil puede aplicar en silencio.
El target restante es sugerencias advisory opt-in sólo después, y
exception-first únicamente tras un cambio de identidad por etapas. Véase
[UX-002E Batch Context Defaults Friction Audit](../../domain/PRICE_LIST_UX_002E_BATCH_CONTEXT_DEFAULTS_FRICTION_AUDIT.md).

### Audit UX-002D — Capture mode friction

La auditoría read-only confirma que `FULL`/Alta y actualización y
`COMPACT`/Actualización compacta son modos persistidos por Version, no sólo
columnas de UI. FULL exige datos de alta y permite NEW; COMPACT exige un
identifier y bloquea no-targets. Matching, trusted history, duplicates,
PARTIAL/COMPLETE, Coverage, canonical naming, Apply y memory son comunes.
La recomendación pendiente de decisión Owner es conservar FULL normal y mover
COMPACT a una opción avanzada, sin derivarlo automáticamente. Véase
[UX-002D Capture Mode Friction Audit](../../domain/PRICE_LIST_UX_002D_CAPTURE_MODE_FRICTION_AUDIT.md).

### UX-002D.1 — Advanced restricted-update option

FULL is now implicit for every fresh load. The primary Composer no longer shows
technical mode radios; its advanced disclosure offers **Solo actualizar
artículos identificados**, with the explicit no-new-items warning. Existing
DRAFT Versions may change mode; Replace persists the validated requested mode
atomically with rows, and INGESTED/READY/APPLIED interpretation remains fixed.
No matching, duplicate, completeness, coverage, Apply or defaults semantics
changed. PostgreSQL proves FULL → COMPACT → FULL and GET persistence, then
proves that an analyzed COMPACT draft retains that mode, rejects reinterpretation
and classifies an unmatched identified row as `INVALID` rather than `NEW`.

### Local iteration — UX-002C Primary review action + secondary draft save

La captura separa controles de vista de acciones de workflow. `Revisar lista`
es la única acción primaria: conserva la orquestación existente de guardar un
snapshot durable y sólo después analizarlo. Si el guardado falla, Analyze no
inicia; si Analyze falla, el snapshot ya guardado sigue recuperable. No hay
autosave ni análisis por edición.

`Guardar para después` sustituye el rótulo técnico anterior y permanece
secundaria, visible únicamente con cambios guardables en captura/borrador. Su
acción sólo persiste el borrador, deja la versión editable y no escribe
reconciliación, memoria, Catalog ni Apply. Al estar limpio se oculta; al editar
vuelve. Después de Analyze se retiran las acciones de captura y se conservan
las acciones de resultado ya existentes (`Reanalizar versión`/`Aplicar lote`)
según lifecycle.

Chrome local usó únicamente QA UX-002A Local: `v3` se guardó, se editó y se
revisó hasta `En revisión` con una resolución y cero atenciones; `v4` se
guardó, recargó y recuperó como `Borrador` editable. La prueba no aplicó ningún
lote. Se verificaron escritorio, 768 y 640 en tema claro; 640 también en
oscuro, incluyendo la disposición primaria/secundaria. Los contratos focales,
typecheck, build, arquitectura y la prueba material PostgreSQL de PBI-041 son
evidencia local para Owner Review, no aceptación ni integración.

### Local remediation — UX-002A.4 Duplicate winner decision failure

El Owner encontró que la tarjeta agrupada de AG `v64` reconocía correctamente
una identidad histórica, pero `Usar fila N` fallaba antes de resolverla. La
auditoría read-only confirmó que ambos `RowDecision` siguieron
`CONFLICT/UNRESOLVED`, el Batch siguió `RECONCILING`, y no hubo cambios de
Catalog, Resolution, Memory, audit ni `published_at`.

La causa primaria fue **payload incorrecto**: la tarjeta reutilizaba el
endpoint de decisión por fila y enviaba el `targetItemId` histórico, pero
omitía el `titleDecision` (`KEEP_CURRENT`) ya derivado por Analyze. El guard
existente `titleDiffers && input.titleDecision === null` rechazaba el comando y
la transacción hacía rollback. La corrección envía únicamente esa decisión
persistida; no pide ni permite que la UI elija una identidad canónica o un
UUID. El repositorio además rechaza un target o decisión de título distintos de
la autoridad del grupo y conserva la exclusión de siblings dentro de la misma
transacción.

La selección sigue siendo pre-Apply: una ganadora queda como única decisión
`APPLY`, sus siblings quedan `EXCLUDE` con
`DUPLICATE_VALUE_CONTRADICTION_SUPERSEDED`, y sólo cambian estados de
reconciliación. Las pruebas PostgreSQL cubren primera, segunda y tercera fila,
reintento stale, target ajeno, reanálisis/reload, exact duplicate sin cambio y
ausencia de escrituras de Catalog/Resolution/Memory/publicación. Chrome local
probó AG `v65` con fila 1 y AG `v66` con fila 2, ambos sin Apply. UX-002B
mantiene su gate explícito de proveedor + intención.

### UX-004.5 — Consistencia de historial por proveedor explorado

El selector **Explorar proveedor** es ahora la única autoridad de presentación
para el historial visible: al elegir una fuente se muestran exclusivamente sus
Versiones, su conteo derivado y su estado vacío real. No hay fallback a AG
después de una selección explícita ni se retienen filas de otra fuente mientras
se cambia de selección. La carga inicial conserva el primer proveedor sólo como
estado inicial; una selección posterior no provoca una recarga dependiente de
la selección, evitando resultados tardíos fuera de orden.

`browseSourceId` sigue separado de `pendingNewLoadSupplierId`: explorar otra
fuente no descarta, modifica ni adopta el proveedor de una carga pendiente. El
diálogo **Nueva carga** sigue comenzando sin proveedor ni intención y requiere
ambas elecciones explícitas. Abrir una versión continúa usando el guard de
trabajo local existente; explorar historial no persiste nada. No cambiaron
API, autorización (`catalog.import.read` conserva lectura), Tenant/Branch,
SupplierSource, SupplierCatalogVersion, policy, Catalog, análisis ni Apply.

La prueba local autenticada verificó AG (71 versiones), Avicell (0 versiones y
empty state), cambios rápidos AG ↔ Avicell ↔ AG, apertura/cierre/restauración
del panel, gate vacío de Nueva carga, Tab/Shift+Tab, 768/640 sin overflow y
temas claro/oscuro. No se guardó, analizó ni aplicó una carga y no hubo
escrituras de datos operativos. Es evidencia local para Owner Review, no
aceptación, integración ni deploy.

### UX-004.6 — Consolidación de acciones de la grid

Las acciones locales **Deshacer**, **Agregar fila** y **Quitar fila activa**
viven ahora una sola vez dentro de la toolbar del workspace, inmediatamente
antes de la acción primaria **Revisar lista**. Los controles de vista quedan a
la izquierda y el flujo de edición/revisión a la derecha; ya no existe una
fila redundante debajo de la toolbar. `Revisar lista` conserva el tono primario,
Agregar fila es secundaria y Deshacer/Quitar son terciarias.

La visibilidad conserva el contrato de preparación y lifecycle: las tres
acciones sólo se renderizan para una grid editable (`DRAFT` o carga local), no
para evidencia histórica/solo lectura. Sus predicados no cambiaron: Deshacer
depende del snapshot local, Agregar fila mantiene el límite de 10,000 y Quitar
fila activa requiere más de una fila. No se modificaron autorización,
política, resize de columnas, Save, Analyze, Apply, API ni persistencia.

Chrome local verificó la secuencia Agregar → Quitar → Deshacer sin guardar;
desktop/1280, 768 y 640 no presentan overflow ni solapamiento horizontal. A
640 las acciones se organizan en dos columnas con Quitar y Revisar a ancho
completo. Un Borrador existente (`AG v43`) mostró los controles con sus estados
deshabilitados correctos; una versión Aplicada (`AG v59`) no expuso acciones de
edición. Claro/oscuro y Tab/Shift+Tab conservaron navegación normal. Es
evidencia local para Owner Review: no hubo Save, Analyze, Apply ni escritura de
datos operativos.

#### Extensión — Gridlines / Cuadrícula

La toolbar de vista incorpora **Cuadrícula** después de Ocultar/Mostrar lista y
antes de las acciones de edición/revisión. Es un botón compacto con
`aria-pressed`; no está condicionado por `catalog.import.prepare`, por lo que
permanece disponible para la evidencia histórica de lectura sin revelar
acciones de edición.

La preferencia se guarda únicamente en `sessionStorage` bajo
`srtaller:bulk-composer:gridlines:v1`, igual que los anchos visuales de la
grid. No forma parte de SupplierSource, SupplierCatalogVersion, policy ni
Catalog; alternarla no modifica `dirty`, undo, Save, Analyze ni Apply. Las
líneas usan bordes de los elementos reales de header/fila/celda y tokens
neutrales: siguen columnas dinámicas y sus anchos normalizados sin imágenes de
fondo, duplicados ni huecos. La selección/foco conserva su inset azul de 2 px
por encima de los bordes.

Chrome local verificó OFF → ON → OFF, grid vacía de una fila y una versión
poblada, ajuste repetido de Título con límites de celda correctos,
Esenciales/Todas, panel de fuentes abierto/cerrado/restaurado, reload de la
preferencia local y `AG v59` aplicada sin controles de edición. Desktop/1280,
768 y 640 pasaron sin overflow de página; claro/oscuro y Space sobre el control
también pasaron. No hubo Save, Analyze, Apply ni escrituras operativas.

### UX-004.7 — Capture Cleanup Ergonomics

La captura normaliza **Marca** sólo en fronteras explícitas: pegado, commit de
edición manual (`blur`) y Aplicar a filas incompletas. Primero reutiliza la
ortografía de una Marca canónica activa cuando su identidad exacta normalizada
coincide; sin referencia, compacta whitespace y sólo ajusta valores de casing
uniforme que son legibles como nombre humano. Tokens cortos en MAYÚSCULAS y
casing mixto intencional se conservan. No hay expansión semántica ni matching
fuzzy: `Moto` nunca se convierte en `Motorola`. El valor observado del proveedor
se mantiene para el payload raw de la carga y se muestra como `Original` cuando
difiere del valor efectivo.

Antes de Analyze, una fila física puede quitarse mediante **Quitar fila activa**
o el control contextual de su encabezado; ambos llaman a la misma operación de
draft. La operación conserva el mínimo de una fila, marca el draft como sucio,
enfoca la misma posición lógica (o la previa al quitar la última) y Deshacer
restaura posición, valores, provenance y selección/foco local. Después de
Analyze no se renderiza esa eliminación física: la evidencia conserva el flujo
existente **Excluir del lote**. No cambiaron matching, duplicados, Analyze,
Apply, capacidades, política de costo cero ni mutaciones de Catalog antes de
Apply.

### UX-005 — First Supplier Baseline Reconciliation Audit

La auditoría read-only de Avicell `v2` preservó sus 744 observaciones y explicó
el resultado `124 NEW / 616 PENDING_REFERENCE / 4 CONFLICT`. La ausencia de un
baseline COMPLETE/APPLIED es correcta para Coverage pero no participa en la
clasificación: los 124 `NEW` tienen Category y Brand canónicas activas; 615
pending sólo carecen de Brand canónica, y una fila adicional tiene Category
mal alineada. No hay target Catalog, candidate, trusted history, identifier ni
fallo REQUIRED que explique el split. Los cuatro conflictos son dos grupos de
duplicados contradictorios y permanecen independientes.

La auditoría preserva la evidencia histórica de Avicell v2. La slice autorizada
**UX-005.1 — New Item Classification with Pending Reference Capture** quedó
materializada localmente: una fila `FULL` sin target ni excepción puede ser
`NEW/APPLY` con Category/Brand canónica, pending existente o capturable de
forma segura. `COMPACT`, required faltante, referencias malformadas, candidatos
y duplicados siguen siendo atención explícita. Analyze no escribe Catalog ni
aprendizaje; Apply conserva la transacción, autorización e idempotencia ya
vigentes. No se reanalizó ni reescribió Avicell v2. Ver
[UX-005 First Supplier Baseline Reconciliation Audit](../../domain/PRICE_LIST_UX_005_FIRST_SUPPLIER_BASELINE_RECONCILIATION_AUDIT.md).

### UX-005.2 — Avicell large-list reanalysis and exception walkthrough

El reanálisis explícito de Avicell v2 verificó materialmente el efecto de
UX-005.1 sin Apply: sus 744 filas quedan `739 NEW / 1 PENDING_REFERENCE / 4
CONFLICT`, o 742 unidades efectivas (`739` resueltas y `3` de atención). Las
615 Brand seguras dejan de bloquear sin crear referencias durante Analyze. Los
dos pares duplicados conservan decisión humana de precio/costo y la fila 618
permanece detenida por Category `V2314 COPIA` malformada. La versión continúa
`RECONCILING`, sin publicación, Resolution, Memory ni cambio de Catalog.

La revisión identificó dos fricciones, sin corregirlas: `Aceptar 1 sugerencia`
marcaría la fila 618 como `APPLY` y podría facilitar su futura captura pending;
además no hay navegación/edición de excepción a fuente en una versión inmutable
de 744 filas. Ver la
[prueba material UX-005.2](../../domain/PRICE_LIST_UX_0052_AVICELL_LARGE_LIST_REANALYSIS_PROOF.md).

### UX-005.3 — Exception-to-source correction navigation

La navegación de una excepción conserva su `rowDecisionId` y revela la fila
física en la cuadrícula, sin depender de una lista filtrada o del número
visible. Las parejas duplicadas de Avicell v2 (`468/469` y `611/612`) se pueden
inspeccionar individualmente; la fila 618 guía a Categoría y mantiene
`V2314 COPIA` como dato del proveedor, sin inferir un valor canónico.

**Corregir fila** prepara una copia local editable. Sólo Guardar o Revisar
lista crea la sucesora explícita de la versión inmutable. Esa sucesora marca al
Batch predecesor como sustituido por corrección: sigue disponible como
evidencia, pero el backend rechaza directamente Analyze, decisiones y Apply
sobre el predecesor. No se cambió Catalog, Resolution, Memory ni AviCell v2 en
la prueba visual. Ver
[UX-005.3 Exception-to-Source Correction Navigation](../../domain/PRICE_LIST_UX_0053_EXCEPTION_TO_SOURCE_CORRECTION_NAVIGATION.md).

### UX-005.4 — Duplicate winner controls preservation

UX-005.4 restituye la frontera complementaria entre **Usar fila N** y **Ir a
fila N**. El segundo sólo navega a evidencia física; el primero ejecuta la
decisión explícita y atómica de una contradicción duplicada. El fix cubre tanto
un target Catalog compartido como una identidad de proveedor nueva sin UUID:
en este último caso, elegir un único ganador crea sólo un `NEW / APPLY`
prospectivo y excluye las copias hermanas. Nunca infiere el valor correcto por
precio, costo u orden.

La evidencia UX-005.4 conserva la frontera previa a publicación. Después, el
Owner eligió explícitamente los ganadores y publicó la sucesora Avicell v3.
La verificación post-Apply conserva la contabilidad `744 → 742`, pero detectó
una fila de precio/costo cero aplicada y que 618 marcas nuevas siguen pending,
no canónicas. PBI-041 no puede avanzar al audit final de cierre hasta decisión
Owner separada sobre ambos hallazgos; no se hizo reparación silenciosa. Ver
[UX-005.5 AviCell Post-Apply Integrity Verification](../../domain/PRICE_LIST_UX_0055_AVICELL_POST_APPLY_INTEGRITY_VERIFICATION.md).
