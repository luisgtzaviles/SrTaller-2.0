/**
 * Derives a human-readable Brand proposal without changing raw provenance,
 * normalized identity, or the canonical Brand authority.
 */
export function normalizeBrandDisplay(value: string, canonicalBrands?: readonly string[]): string;
