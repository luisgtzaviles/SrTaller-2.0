import { sql, type Kysely } from 'kysely';
import type { DatabaseSchema } from '../database-types.js';
const added = ['catalog.configuration.read', 'catalog.configuration.manage'] as const;
const prior = ['users.read', 'users.manage', 'access_matrix.read', 'access_matrix.manage', 'repairs.read', 'repairs.add_note', 'repairs.create', 'repairs.correct_intake', 'repairs.classify', 'repairs.configuration.read', 'repairs.configuration.manage', 'repairs.catalogs.read', 'repairs.catalogs.manage', 'price_list.read', 'catalog.manage', 'catalog.prices.manage', 'catalog.branch_prices.manage', 'catalog.reference_cost.read', 'catalog.reference_cost.manage', 'catalog.import.prepare', 'catalog.import.publish', 'catalog.items.bulk_retire', 'catalog.suppliers.delete'] as const;
async function replaceConstraint(database: Kysely<DatabaseSchema>, values: readonly string[]): Promise<void> { await sql`alter table access_capabilities drop constraint access_capabilities_code_ck`.execute(database); await sql.raw(`alter table access_capabilities add constraint access_capabilities_code_ck check (capability_code in (${values.map((value) => `'${value}'`).join(', ')}))`).execute(database); }
export async function up(database: Kysely<DatabaseSchema>): Promise<void> { await replaceConstraint(database, [...prior, ...added]); await database.insertInto('access_capabilities').values(added.map((capability_code) => ({ capability_code, created_at: new Date('2026-09-17T19:01:00.000Z') }))).onConflict((conflict) => conflict.column('capability_code').doNothing()).execute(); }
export async function down(database: Kysely<DatabaseSchema>): Promise<void> { await database.deleteFrom('access_role_capabilities').where('capability_code', 'in', [...added]).execute(); await database.deleteFrom('access_capabilities').where('capability_code', 'in', [...added]).execute(); await replaceConstraint(database, prior); }
